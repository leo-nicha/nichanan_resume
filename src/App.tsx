import { ResumeSinglePage } from "./ResumeSinglePage.tsx";

export default function App() {
    return (
        <div className="min-h-screen bg-gray-50">
            <ResumeSinglePage />
        </div>
    );
}